#!/bin/bash
set -e

engine=${EYEPROLOG:-eyeprolog}

# EyeProlog executes :+/2 natively; library(eyelet) supplies stable/1 and
# becomes/2 through the normal bundled-library autoloader.
exec "$engine" "$@"
