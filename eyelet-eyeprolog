#!/bin/bash
set -e

engine=${EYEPROLOG:-eyeprolog}

# EyeProlog autoloads its bundled Prolog library(eyelet) driver for :+/2,
# including stable/1 and becomes/2 through the normal library autoloader.
exec "$engine" "$@"
